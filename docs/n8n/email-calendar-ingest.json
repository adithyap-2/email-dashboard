{
  "name": "Email + Calendar Ingest → /ingest/emails,/ingest/meetings",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "minutes",
              "minutesInterval": 30
            }
          ]
        }
      },
      "id": "b0000000-0000-4000-8000-000000000000",
      "name": "Every 30m",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        -520,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "// TODO: list your team's mailboxes and internal domains.\nconst users = ['pg@crid.org'];\nconst internal = ['crid.org'];\nconst iso = (d) => d.toISOString().split('.')[0] + 'Z';\nconst now = new Date();\nconst sinceIso = iso(new Date(now.getTime() - 30 * 864e5));\nconst calStart = iso(new Date(now.getTime() - 30 * 864e5));\nconst calEnd = iso(new Date(now.getTime() + 30 * 864e5));\nreturn users.map(u => ({ json: { user: u, internal, sinceIso, calStart, calEnd } }));"
      },
      "id": "b1000000-0000-4000-8000-000000000001",
      "name": "Config",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -300,
        0
      ]
    },
    {
      "parameters": {
        "url": "=https://graph.microsoft.com/v1.0/users/{{ $json.user }}/mailFolders/inbox/messages",
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "$filter",
              "value": "=receivedDateTime ge {{ $json.sinceIso }}"
            },
            {
              "name": "$select",
              "value": "id,subject,from,receivedDateTime,bodyPreview"
            },
            {
              "name": "$top",
              "value": "999"
            }
          ]
        },
        "options": {}
      },
      "id": "b2000000-0000-4000-8000-000000000002",
      "name": "Graph: received",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -80,
        -200
      ],
      "credentials": {
        "oAuth2Api": {
          "id": "REPLACE_WITH_GRAPH_APP_ONLY_CRED",
          "name": "Graph App-Only"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// One Graph response per mailbox arrives here. Emit one payload per mailbox:\n// reading $json would silently keep only the FIRST and drop every other user.\nconst cfgAll = $('Config').all();\nconst dom = (e) => (e && e.includes('@')) ? e.split('@')[1].toLowerCase() : '';\nreturn $input.all().map((item, i) => {\n  const cfg = (cfgAll[i] || cfgAll[0]).json;\n  const owner = cfg.user;\n  const internal = cfg.internal.map(d => d.toLowerCase());\n  const isExt = (e) => { const d = dom(e); return !!d && !internal.includes(d); };\n  const payload = (item.json.value || []).map(m => {\n    const from = (m.from && m.from.emailAddress) || {};\n    return { id: m.id, owner, direction: 'received', subject: m.subject || null,\n      preview: (m.bodyPreview || '').slice(0, 160), contact_name: from.name || null,\n      contact_email: from.address || null, organisation: null,\n      is_external: isExt(from.address), ts: m.receivedDateTime };\n  });\n  return { json: { payload } };\n});"
      },
      "id": "b3000000-0000-4000-8000-000000000003",
      "name": "Normalise received",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        160,
        -200
      ]
    },
    {
      "parameters": {
        "url": "=https://graph.microsoft.com/v1.0/users/{{ $json.user }}/mailFolders/sentitems/messages",
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "$filter",
              "value": "=sentDateTime ge {{ $json.sinceIso }}"
            },
            {
              "name": "$select",
              "value": "id,subject,toRecipients,sentDateTime,bodyPreview"
            },
            {
              "name": "$top",
              "value": "999"
            }
          ]
        },
        "options": {}
      },
      "id": "b4000000-0000-4000-8000-000000000004",
      "name": "Graph: sent",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -80,
        0
      ],
      "credentials": {
        "oAuth2Api": {
          "id": "REPLACE_WITH_GRAPH_APP_ONLY_CRED",
          "name": "Graph App-Only"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// One Graph response per mailbox arrives here. Emit one payload per mailbox:\n// reading $json would silently keep only the FIRST and drop every other user.\nconst cfgAll = $('Config').all();\nconst dom = (e) => (e && e.includes('@')) ? e.split('@')[1].toLowerCase() : '';\nreturn $input.all().map((item, i) => {\n  const cfg = (cfgAll[i] || cfgAll[0]).json;\n  const owner = cfg.user;\n  const internal = cfg.internal.map(d => d.toLowerCase());\n  const isExt = (e) => { const d = dom(e); return !!d && !internal.includes(d); };\n  const payload = (item.json.value || []).map(m => {\n    const recips = (m.toRecipients || []).map(r => r.emailAddress || {});\n    const ext = recips.filter(r => isExt(r.address));\n    const c = ext[0] || recips[0] || {};\n    return { id: m.id, owner, direction: 'sent', subject: m.subject || null,\n      preview: (m.bodyPreview || '').slice(0, 160), contact_name: c.name || null,\n      contact_email: c.address || null, organisation: null,\n      is_external: ext.length > 0, ts: m.sentDateTime };\n  });\n  return { json: { payload } };\n});"
      },
      "id": "b5000000-0000-4000-8000-000000000005",
      "name": "Normalise sent",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        160,
        0
      ]
    },
    {
      "parameters": {
        "url": "=https://graph.microsoft.com/v1.0/users/{{ $json.user }}/calendarView",
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "startDateTime",
              "value": "={{ $json.calStart }}"
            },
            {
              "name": "endDateTime",
              "value": "={{ $json.calEnd }}"
            },
            {
              "name": "$select",
              "value": "id,subject,organizer,attendees,start,end,location"
            },
            {
              "name": "$top",
              "value": "999"
            }
          ]
        },
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Prefer",
              "value": "outlook.timezone=\"UTC\""
            }
          ]
        },
        "options": {}
      },
      "id": "b6000000-0000-4000-8000-000000000006",
      "name": "Graph: calendar",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -80,
        200
      ],
      "credentials": {
        "oAuth2Api": {
          "id": "REPLACE_WITH_GRAPH_APP_ONLY_CRED",
          "name": "Graph App-Only"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// One Graph response per mailbox arrives here. Emit one payload per mailbox:\n// reading $json would silently keep only the FIRST and drop every other user.\nconst cfgAll = $('Config').all();\nconst dom = (e) => (e && e.includes('@')) ? e.split('@')[1].toLowerCase() : '';\nreturn $input.all().map((item, i) => {\n  const cfg = (cfgAll[i] || cfgAll[0]).json;\n  const owner = cfg.user;\n  const internal = cfg.internal.map(d => d.toLowerCase());\n  const isExt = (e) => { const d = dom(e); return !!d && !internal.includes(d); };\n  const addr = (p) => (p && p.emailAddress) || {};\n  const payload = (item.json.value || []).map(ev => {\n    const parties = (ev.attendees || []).map(addr);\n    const org = addr(ev.organizer);\n    const ext = parties.filter(p => isExt(p.address));\n    if (isExt(org.address)) ext.unshift(org);\n    const c = ext[0] || (org.address ? org : (parties[0] || {}));\n    return { id: ev.id, owner, subject: ev.subject || null, organisation: null,\n      contact_name: c.name || null, contact_email: c.address || null,\n      is_external: ext.length > 0,\n      start_ts: (ev.start || {}).dateTime ? (ev.start.dateTime.split('.')[0] + 'Z') : null,\n      end_ts: (ev.end || {}).dateTime ? (ev.end.dateTime.split('.')[0] + 'Z') : null,\n      location: (ev.location || {}).displayName || null,\n      attendees: parties.map(p => p.name ? (p.name + ' <' + p.address + '>') : p.address),\n      followup_status: 'none' };\n  });\n  return { json: { payload } };\n});"
      },
      "id": "b7000000-0000-4000-8000-000000000007",
      "name": "Normalise meetings",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        160,
        200
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.N8N_BACKEND_URL }}/ingest/emails",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ $json.payload }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {}
      },
      "id": "b8000000-0000-4000-8000-000000000008",
      "name": "POST /ingest/emails",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        420,
        -100
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "REPLACE_WITH_INGEST_KEY_CRED",
          "name": "Ingest Key"
        }
      }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.N8N_BACKEND_URL }}/ingest/meetings",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ $json.payload }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {}
      },
      "id": "b9000000-0000-4000-8000-000000000009",
      "name": "POST /ingest/meetings",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        420,
        200
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "REPLACE_WITH_INGEST_KEY_CRED",
          "name": "Ingest Key"
        }
      }
    }
  ],
  "connections": {
    "Every 30m": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Config": {
      "main": [
        [
          {
            "node": "Graph: received",
            "type": "main",
            "index": 0
          },
          {
            "node": "Graph: sent",
            "type": "main",
            "index": 0
          },
          {
            "node": "Graph: calendar",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Graph: received": {
      "main": [
        [
          {
            "node": "Normalise received",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalise received": {
      "main": [
        [
          {
            "node": "POST /ingest/emails",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Graph: sent": {
      "main": [
        [
          {
            "node": "Normalise sent",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalise sent": {
      "main": [
        [
          {
            "node": "POST /ingest/emails",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Graph: calendar": {
      "main": [
        [
          {
            "node": "Normalise meetings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalise meetings": {
      "main": [
        [
          {
            "node": "POST /ingest/meetings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "active": false
}